@page "/"

<PageTitle>UltraChess</PageTitle>

<div class="page-shell home-page">
    <section class="home-hero app-card">
        <div class="home-hero__pattern" aria-hidden="true"></div>

        <div class="home-hero__copy">
            <MudText Typo="Typo.h2" Class="home-hero__title">Играй, анализируй и прокачивай свои навыки с помощью UltraChess.</MudText>

            <MudText Typo="Typo.body1" Class="home-hero__subtitle">
                Выбери уровень соперника, сыграй партию и сразу получи разбор ходов от ИИ.
            </MudText>

            <div class="home-hero__actions">
                <MudButton Variant="Variant.Filled" Color="Color.Success" Size="Size.Large" StartIcon="@Icons.Material.Filled.SportsEsports" Href="/playwithai">
                    Играть с ИИ
                </MudButton>
                <MudButton Variant="Variant.Outlined" Color="Color.Success" Size="Size.Large" StartIcon="@Icons.Material.Filled.Insights" Href="/analysis">
                    ИИ анализ партий
                </MudButton>
            </div>
        </div>
    </section>

    <section class="home-steps app-card">
        <div class="home-step">
            <span class="home-step__num">1</span>
            <MudIcon Icon="@Icons.Material.Filled.Tune" Color="Color.Success" />
            <div class="home-step__copy">
                <MudText Typo="Typo.subtitle2" Class="home-step__title">Настройте уровень</MudText>
                <MudText Typo="Typo.caption" Class="home-step__text">20 уровней - от новичка до эксперта</MudText>
            </div>
        </div>

        <MudIcon Icon="@Icons.Material.Filled.ArrowForward" Class="home-step__arrow" />

        <div class="home-step">
            <span class="home-step__num">2</span>
            <MudIcon Icon="@Icons.Material.Filled.VideogameAsset" Color="Color.Info" />
            <div class="home-step__copy">
                <MudText Typo="Typo.subtitle2" Class="home-step__title">Сыграйте партию</MudText>
                <MudText Typo="Typo.caption" Class="home-step__text">Прямо в браузере, без установки</MudText>
            </div>
        </div>

        <MudIcon Icon="@Icons.Material.Filled.ArrowForward" Class="home-step__arrow" />

        <div class="home-step">
            <span class="home-step__num">3</span>
            <MudIcon Icon="@Icons.Material.Filled.QueryStats" Color="Color.Warning" />
            <div class="home-step__copy">
                <MudText Typo="Typo.subtitle2" Class="home-step__title">Получите анализ</MudText>
                <MudText Typo="Typo.caption" Class="home-step__text">ИИ разберёт каждый ваш ход</MudText>
            </div>
        </div>
    </section>

    <section class="responsive-grid home-features">
        <MudPaper Elevation="0" Class="app-card home-feature">
            <MudIcon Icon="@Icons.Material.Filled.AutoGraph" Color="Color.Success" Size="Size.Large" Class="mb-2" />
            <MudText Typo="Typo.h6">Играй и анализируй партии</MudText>
            <MudText Typo="Typo.body2" Class="home-feature__text">
                Играйте и разберайте партии с помощью мощного движка Stockfish 18 Lite, который можно настроить под ваш уровень игры.
            </MudText>
        </MudPaper>

        <MudPaper Elevation="0" Class="app-card home-feature">
            <MudIcon Icon="@Icons.Material.Filled.Psychology" Color="Color.Info" Size="Size.Large" Class="mb-2" />
            <MudText Typo="Typo.h6">20 уровней сложности</MudText>
            <MudText Typo="Typo.body2" Class="home-feature__text">
                Гибкая настройка сложности от новичка до эксперта позволяет каждому найти подходящего соперника и улучшать свои навыки.
            </MudText>
        </MudPaper>

        <MudPaper Elevation="0" Class="app-card home-feature">
            <MudIcon Icon="@Icons.Material.Filled.Person" Color="Color.Warning" Size="Size.Large" Class="mb-2" />
            <MudText Typo="Typo.h6">Личный профиль</MudText>
            <MudText Typo="Typo.body2" Class="home-feature__text">
                Храните статистику, рейтинг, аватар и историю партий в одном месте без хаотичных экранов.
            </MudText>
        </MudPaper>

        <MudPaper Elevation="0" Class="app-card home-feature">
            <MudIcon Icon="@Icons.Material.Filled.Public" Color="Color.Primary" Size="Size.Large" Class="mb-2" />
            <MudText Typo="Typo.h6">Без установки</MudText>
            <MudText Typo="Typo.body2" Class="home-feature__text">
                Играйте и анализируйте партии прямо в браузере — на компьютере или телефоне, без скачивания приложений.
            </MudText>
        </MudPaper>
    </section>
</div>

<style>
    .home-page {
        display: flex;
        flex-direction: column;
        gap: 1.1rem;
        padding-block: 0.5rem 1rem;
        height: 100%;
        min-height: 0;
    }

    .home-hero {
        position: relative;
        overflow: hidden;
        display: flex;
        align-items: center;
        padding: clamp(1.4rem, 3vw, 2rem) clamp(1.4rem, 4vw, 2.5rem);
        background:
            radial-gradient(circle at top right, color-mix(in srgb, var(--app-accent-2) 16%, transparent), transparent 55%),
            linear-gradient(135deg, color-mix(in srgb, var(--app-surface) 95%, transparent), color-mix(in srgb, var(--app-surface-2) 92%, transparent));
    }

    .home-hero__pattern {
        position: absolute;
        inset: 0;
        width: 46%;
        margin-left: auto;
        background-image:
            linear-gradient(45deg, color-mix(in srgb, var(--app-accent) 10%, transparent) 25%, transparent 25%, transparent 75%, color-mix(in srgb, var(--app-accent) 10%, transparent) 75%),
            linear-gradient(45deg, color-mix(in srgb, var(--app-accent) 10%, transparent) 25%, transparent 25%, transparent 75%, color-mix(in srgb, var(--app-accent) 10%, transparent) 75%);
        background-size: 44px 44px;
        background-position: 0 0, 22px 22px;
        -webkit-mask-image: linear-gradient(to left, #000 20%, transparent 85%);
        mask-image: linear-gradient(to left, #000 20%, transparent 85%);
        pointer-events: none;
    }

    .home-hero__copy {
        position: relative;
        z-index: 1;
        max-width: 46rem;
    }

    .home-hero__title {
        font-size: clamp(1.6rem, 2.7vw, 2.4rem);
        font-weight: 800;
        line-height: 1.18;
        margin: 0;
    }

    .home-hero__subtitle {
        margin-top: 0.65rem;
        max-width: 42ch;
        color: var(--app-muted);
    }

    .home-hero__actions {
        display: flex;
        gap: 0.75rem;
        flex-wrap: wrap;
        margin-top: 1.25rem;
    }

    .home-steps {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.75rem;
        padding: 0.85rem 1.5rem;
    }

    .home-step {
        display: flex;
        align-items: center;
        gap: 0.65rem;
        min-width: 0;
        flex: 1;
    }

    .home-step__num {
        flex-shrink: 0;
        width: 1.5rem;
        height: 1.5rem;
        border-radius: 50%;
        display: grid;
        place-items: center;
        font-size: 0.78rem;
        font-weight: 700;
        color: var(--app-accent);
        background: color-mix(in srgb, var(--app-accent) 16%, transparent);
    }

    .home-step__copy {
        display: flex;
        flex-direction: column;
        gap: 0;
        min-width: 0;
    }

    .home-step__title {
        font-weight: 700;
    }

    .home-step__text {
        color: var(--app-muted);
        white-space: nowrap;
    }

    .home-step__arrow {
        flex-shrink: 0;
        color: var(--app-border);
    }

    .home-features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1rem;
    }

    .home-feature {
        padding: 1.1rem 1.25rem;
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
        transition: transform 0.18s ease, box-shadow 0.18s ease;
    }

    .home-feature:hover {
        transform: translateY(-2px);
    }

    .home-feature__text {
        color: var(--app-muted);
        line-height: 1.45;
    }

    @@media (max-width: 920px) {
        .home-hero__pattern {
            display: none;
        }

        .home-hero__copy {
            max-width: none;
        }
    }

    @@media (max-width: 760px) {
        .home-steps {
            flex-direction: column;
            align-items: flex-start;
        }

        .home-step__arrow {
            display: none;
        }

        .home-step__text {
            white-space: normal;
        }
    }

    @@media (max-width: 600px) {
        .home-hero__actions {
            flex-direction: column;
        }

        .home-hero__actions .mud-button-root {
            width: 100%;
        }
    }
</style>
